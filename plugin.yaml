name: a0_comms
title: Inter-Session Messenger
description: "Async + direct messaging between agent sessions across projects. 8 actions: send, check, check_session, reply, list_sessions, switch_session, broadcast, forward. Telegram-ready with session binding and natural language command routing."
version: 1.0.0
settings_sections:
  - agent
per_project_config: true
per_agent_config: true
